/**
 * 테스트에서 `import "server-only"` 를 대신하는 빈 모듈.
 *
 * 진짜 server-only는 클라이언트 번들에 섞이면 터지라고 있는 표시다. 그
 * 장치가 테스트에서도 걸려서 서버 전용 파일을 아예 불러올 수 없었다.
 * vitest.config.mts에서만 이 파일로 바꿔치기하므로, 실제 빌드의 보호는
 * 그대로 남는다.
 */
export {};
