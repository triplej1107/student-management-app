-- 강의 결석/지각 사유.
--
-- 두 가지 경로로 채워진다.
--   1) 맥가이7 등하원명단의 "결석/지각사유" 칸을 자동으로 가져온다
--      (예: "일요일", "가족여행"). 응답의 SCHEINFO 칸에 $로 이어져 온다:
--        "09:00 장종주$Y$5350283$일요일"
--          [1] Y면 출석, 비어 있으면 미출석
--          [3] 사유 (없으면 빈 값)
--   2) 조교가 앱의 강의 출결 화면에서 직접 적는다.
--
-- 지금은 사유가 맥가이7에만 있어서, 앱만 보면 왜 안 왔는지 알 수가 없다.
-- 나중에 "그때 뭐라고 했었지"를 되짚을 수 있어야 반복해서 둘러대는 학생을
-- 알아볼 수 있다.
--
-- 사람이 적어둔 값을 자동 동기화가 덮으면 안 된다 — 조교가 더 자세히 적어둔
-- 사정이 맥가이7의 짧은 메모로 지워지기 때문. 채우기 규칙은 코드에서 다룬다.

alter table lecture_attendance
  add column if not exists absence_reason text;

comment on column lecture_attendance.absence_reason is
  '강의 결석/지각 사유. 맥가이7에서 자동으로 들어오거나 조교가 직접 적는다.';
